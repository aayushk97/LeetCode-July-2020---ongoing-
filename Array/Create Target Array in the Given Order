class Solution {
public:
    vector<int> createTargetArray(vector<int>& nums, vector<int>& index) {
        vector<int> finalResult;
        
        for(int i = 0; i < nums.size(); i++) finalResult.insert(finalResult.begin()+index[i], nums[i]);
        
        return finalResult;
    }
};
